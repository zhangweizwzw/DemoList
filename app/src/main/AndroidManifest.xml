<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zw.demolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WeChatDHActivity" />
        <activity android:name=".QqSlideMenuActivity" />
        <activity android:name=".ManySlideFragmentActivity" />
        <activity
            android:name=".RightSlideActivity"
            android:theme="@style/SlideClose" />
        <activity android:name=".ButterKnifeActivity" />
        <activity android:name=".UserfulActivity" />
        <activity android:name=".BigBangActivity" />
        <activity android:name=".BlurActivity" />
        <activity android:name=".BannerActivity" />
        <activity android:name=".widget.HideToolBarActivity"></activity>
    </application>

</manifest>